pour executer base de donne sur docker :

cd docker   

docker-compose up -d 

pour cree une image dans docker posgress.yaml 

docker compose -f posgress.yaml up -d  

problem : le conteneur cree mais n est pas demare 

docker start caboinet_postress